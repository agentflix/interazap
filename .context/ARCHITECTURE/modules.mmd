graph LR
    subgraph Modules["Módulos do InteraZap"]
        SHARED["Shared Kernel"]
        AUTH["Auth"]
        PLATFORM["Platform"]
        CHAT["Chat"]
        CRM["CRM"]
        BILLING["Billing"]
        AI["AI"]
        GATEWAY["Gateway"]
        DASHBOARD["Dashboard"]
        REPORTS["Reports"]
        CONFIG["Configuration"]
    end

    SHARED -->|shared| AUTH
    SHARED -->|shared| PLATFORM
    SHARED -->|shared| CHAT
    SHARED -->|shared| CRM
    SHARED -->|shared| BILLING
    SHARED -->|shared| AI
    SHARED -->|shared| GATEWAY
    SHARED -->|shared| DASHBOARD
    SHARED -->|shared| REPORTS
    SHARED -->|shared| CONFIG

    AUTH -->|uses| SHARED
    PLATFORM -->|uses| SHARED
    CHAT -->|depends| AUTH
    CHAT -->|uses| GATEWAY
    CRM -->|depends| AUTH
    BILLING -->|depends| AUTH
    AI -->|independent| SHARED
    GATEWAY -->|depends| CHAT
    DASHBOARD -->|aggregates| CHAT
    DASHBOARD -->|aggregates| BILLING
    REPORTS -->|aggregates| CHAT
    REPORTS -->|aggregates| BILLING
    REPORTS -->|aggregates| CRM
    CONFIG -->|depends| AUTH

    style SHARED fill:#FFF9C4,stroke:#F9A825
