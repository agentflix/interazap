# Changelog — 2026-04-29

> Registro factual do que mudou. Para decisões e contexto, veja MEMORY.

## Entradas

- [12:05] FIX [chat]: Normalização de tipo de mídia no envio de mensagens de ticket
    - Ajustada a criação de mensagens para normalizar `type` com base em `mime_type` e extensão de arquivo/URL.
    - Evita persistir e enviar imagens como `document`/`file` quando houver evidência de mídia visual.
    - Arquivos: `api/src/Domain/Chat/Actions/SendChatMessageAction.php`
    - Ref: BUGFIX chat-media-inline-image

- [12:05] TEST [chat]: Regressão para mensagem com `type=document` e `mime_type=image/*`
    - Adicionado teste feature cobrindo POST `tickets/{ticketId}/messages` com mídia de imagem para validar persistência em `type=image`.
    - Valida também payload enviado ao gateway com `type=image`.
    - Arquivos: `api/tests/Feature/ChatMessageControllerTest.php`
    - Ref: BUGFIX chat-media-inline-image

- [12:18] FIX [webchat]: Bloqueio de PDF no modo Foto com validação defensiva de anexo
    - Mesmo com `accept=image/*`, alguns fluxos do seletor permitiam escolher arquivo incompatível; agora o `onFileSelected` valida MIME/extensão contra o tipo selecionado (Foto/Vídeo/Áudio/Documento) antes do upload.
    - Corrigida condição de corrida no primeiro clique: o `accept` agora é aplicado diretamente no `input[type=file]` antes de abrir o seletor, eliminando necessidade de clicar duas vezes.
    - Arquivos: `app/src/app/pages/webchat/components/chat-window/chat-window.component.ts`
    - Ref: BUGFIX webchat-attachment-filter

- [12:18] FIX [webchat/api]: Rejeição de payload com `type` e `mime_type` incompatíveis
    - Adicionada regra no endpoint `POST /api/webchat/messages` para recusar inconsistência (ex.: `type=image` com `mime_type=application/pdf`).
    - Arquivos: `api/src/Domain/Chat/Http/Controllers/WebChatMessageController.php`
    - Ref: BUGFIX webchat-attachment-filter

- [12:18] TEST [webchat]: Regressões para filtro de anexo e validação de compatibilidade
    - Frontend: novo teste garantindo bloqueio de PDF no modo Foto e aceite de imagem válida.
    - Backend: novo teste garantindo HTTP 400 para `mime_type` incompatível com `type`.
    - Arquivos: `app/src/app/pages/webchat/components/chat-window/chat-window.component.spec.ts`, `api/tests/Feature/Chat/WebChatMessageControllerTest.php`
    - Ref: BUGFIX webchat-attachment-filter

- [14:50] FIX [admin/queues]: Fallback de leitura para endpoint de health quando `/api/admin/queues/overview` nao existe
    - `QueueService.getOverview()` agora tenta `/api/admin/queues/overview` e, em erro (ex.: 404), usa `/api/health/queues` com mapeamento para o contrato da tela.
    - `QueueService.getQueueMetrics()` recebeu fallback equivalente para `/api/health/queues/{queue}`.
    - Arquivos: `app/src/app/core/services/queue.service.ts`
    - Ref: BUGFIX queue-dashboard-empty-state

- [14:50] TEST [admin/queues]: Cobertura de fallback do QueueService
    - Adicionada spec cobrindo: caminho principal, fallback de overview e fallback de metricas por fila.
    - Arquivos: `app/src/app/core/services/queue.service.spec.ts`
    - Ref: BUGFIX queue-dashboard-empty-state

- [15:45] FIX [chat/rbac]: Admin redirecionado ao abrir lista de transmissão
    - Adicionadas permissões `chat.transmission_lists.*` ao seeder base de permissões para garantir disponibilidade no catálogo principal.
    - Atualizado seeder de papéis para conceder permissões de lista de transmissão para `inquilino` e `gerente`.
    - Criada migration de backfill para ambientes já existentes, concedendo permissões aos papéis administrativos (`super-admin`, `admin`, `inquilino`, `gerente`).
    - Arquivos: `api/database/seeders/AuthPermissionSeeder.php`, `api/database/seeders/RolePermissionSeeder.php`, `api/database/migrations/2026_04_29_090000_backfill_chat_transmission_list_permissions.php`
    - Ref: BUGFIX chat-transmission-list-admin-redirect

- [15:45] TEST [auth/rbac]: Regressão de permissões de transmissão
    - Adicionados testes cobrindo presença de permissões de lista de transmissão no `AuthPermissionSeeder`.
    - Adicionados testes cobrindo concessão de permissões de lista de transmissão para papéis administrativos no `RolePermissionSeeder`.
    - Arquivos: `api/tests/Feature/AuthRbacTest.php`
    - Ref: BUGFIX chat-transmission-list-admin-redirect

- [16:18] REFACTOR [chat/frontend]: FEAT-049 — Decomposição do host `chat.ts` em 4 services dedicados
    - Extraídos `ChatRealtimeListenerService`, `ChatTicketTransferService`, `ChatTicketCloseService`, `ChatRecordingDispatcherService`.
    - Host reduzido de 1.333 → 1.078 linhas (-19%); CA-001 (≤700) parcial — follow-up rastreado em FEAT-050 (mídia/anexos + route-sync).
    - Arquivos: `app/src/app/pages/chat/services/chat-realtime-listener.service.ts`, `chat-ticket-transfer.service.ts`, `chat-ticket-close.service.ts`, `chat-recording-dispatcher.service.ts` (+ specs), `app/src/app/pages/chat/chat.ts`
    - Refs: FEAT-049, `.context/DOCS/FEATURES/FEAT-049-chat-host-decomposition.md`, `.context/DOCS/TASKS/FEAT-049-tasks.md`

- [16:18] FIX [chat/frontend]: Lifecycle bug em `ChatRealtimeListenerService` (singleton + flag `started`)
    - Removido flag `started` que impedia re-subscribe nos 3 listeners (`message.received`, `chat.activity`, `chat.ticket.new`) após destroy/remount do componente Chat.
    - Cada chamada `start(destroyRef)` agora reassina ligado ao DestroyRef do caller; `realtime.connect()` permanece idempotente no nível do Socket.IO.
    - Adicionado teste de regressão `re-subscribes after the previous DestroyRef is destroyed (remount)`.
    - Arquivos: `app/src/app/pages/chat/services/chat-realtime-listener.service.ts`, `chat-realtime-listener.service.spec.ts`
    - Ref: FEAT-049 (post-review correction)

- [16:18] CHORE [chat/frontend]: Lint scope FEAT-049 zerado
    - Corrigidos 10 erros: 8 `consistent-type-imports`, 1 `array-type`, 1 `class-literal-property-style`.
    - Arquivos: 4 services + 4 specs + `chat.spec.ts` (PlatformServiceStub: getter → readonly field).
    - Ref: FEAT-049

- [16:18] DOCS [chat/frontend]: Sincronização de feature doc e tasks doc da FEAT-049
    - Tabela de tasks atualizada para 6/6 ✅; CA-003 17/17; CA-004 ✅ pós-correção.
    - Arquivos: `.context/DOCS/FEATURES/FEAT-049-chat-host-decomposition.md`, `.context/DOCS/TASKS/FEAT-049-tasks.md`
    - Ref: FEAT-049
