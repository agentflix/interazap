# Changelog — 2026-04-29

> Registro factual do que mudou. Para decisões e contexto, veja MEMORY.

---

## Entradas

- [19:30] FEAT [chat/composer]: Implementação do Sprint 4 — Composer condicional com janela 24h (FEAT-050 D1–D7)
  - D1: `TemplateSelectorComponent` movido de `pages/chat/components/new-conversation-modal/components/template-selector/` para `shared/components/template-selector/`, tornando-o reutilizável em qualquer parte da aplicação. Exports atualizados em `shared/components/index.ts`.
  - D2: `ChatStore` estendido com `instanceProviders` (mapa instance_id → provider), `windowStatus` signal e `composerMode` computed (`free` | `mixed` | `template-only`).
  - D3: Efeito no `chat.ts` que observa `selectedTicket` e carrega `windowStatus` via `WindowVerificationService` quando o provider da instância é `meta`.
  - D4/D5: `ChatConversationComponent` adaptado para renderizar 3 modos do composer:
    - `free`: textarea normal (sem mudança visual).
    - `mixed`: textarea + botão inline de template (popover com `af-popover`) + chip de resumo quando template selecionado.
    - `template-only`: `af-banner` warning + CTA "Selecionar template" que abre `af-modal` com `TemplateSelectorComponent`.
  - D6: `sendMessage()` no `chat.ts` bifurcado entre envio de texto livre (via `MessageSendService`) e envio de template (via `CalledMessageService.sendTemplate()`). Conversão de parâmetros `Record<string, string>` para formato de componentes Meta implementada.
  - D7: Handler no `chat.ts` escuta `ChatRealtimeListenerService.incomingMessage$` e, ao receber inbound do ticket selecionado, invalida o cache da janela 24h e re-checa o status automaticamente.
  - Testes: `chat.store.spec.ts` expandido com 6 novos testes para `composerMode` e `windowStatus`. `chat-conversation-component.spec.ts` criado com 5 testes cobrindo os 3 modos do composer e eventos de template.
  - Arquivos principais:
    - `app/src/app/shared/components/template-selector/*` (novo path)
    - `app/src/app/pages/chat/chat.store.ts`
    - `app/src/app/pages/chat/chat.ts`
    - `app/src/app/pages/chat/chat.html`
    - `app/src/app/pages/chat/components/chat-conversation-component/chat-conversation-component.ts`
    - `app/src/app/pages/chat/components/chat-conversation-component/chat-conversation-component.html`
    - `app/src/app/pages/chat/components/chat-conversation-component/chat-conversation-component.spec.ts` (novo)
    - `app/src/app/pages/chat/services/chat-realtime-listener.service.ts`
    - `app/src/app/core/services/called.service.ts` (adição de `instance_id` à interface `Called`)
  - Ref: FEAT-050 / TASK-050.D1–D7

- [22:45] QA [chat/templates]: Sprint 5 — Gates de validação e evidências dos ACs (FEAT-050 E1–E3)
  - E1: Verificação estrutural dos testes Pest backend (10 arquivos cobrindo CRUD, actions, jobs, listeners, webhooks). PHPStan L9 sem novos erros.
  - E2: Build production ✅. ESLint 0 erros nos arquivos alterados. Vitest passando para `chat.store.spec.ts`, `chat-conversation-component.spec.ts` e `template-selector.spec.ts`.
  - E3: Documentação de evidências dos 12 critérios de aceitação (AC-01 a AC-12) validados via implementação + testes unitários.
  - Arquivos:
    - `.context/DOCS/CHANGELOG/2026-04-29-qa-evidence-feat-050-sprint5.md`
  - Ref: FEAT-050 / TASK-050.E1–E3

- [23:15] FIX [chat/composer]: Correções pós-QA/REVIEWER (bugs críticos)
  - **CRÍTICO #1**: `buildTemplateComponents()` usava `localeCompare()` para ordenação de parâmetros (lexicográfica, não numérica). Substituído por `parseInt` para templates com 10+ parâmetros não terem valores trocados.
  - **CRÍTICO #2**: `TemplateSelectorComponent` não tinha botão de submit — o método `submit()` nunca era chamado. Adicionado botão "Enviar template" no HTML + import do `ButtonComponent`.
  - **MÉDIO**: `chatInstanceId ?? ''` causava chamada de API inválida com `channelId` vazio. Corrigido com `@if (chatInstanceId())` nos wrappers do popover e modal.
  - **QA**: `beforeEach` do `chat-conversation-component.spec.ts` corrigido com `selectedTicketId` para garantir que o DOM do footer seja renderizado.
  - Arquivos: `chat.ts`, `template-selector.ts`, `template-selector.html`, `chat-conversation-component.html`, `chat-conversation-component.spec.ts`
  - Ref: FEAT-050 Sprint 5 review

- [20:00] FIX [gateway/push]: Correções de erros no gateway (FEAT-047 Fase 5)
  - **CRÍTICO #1**: `composer.json` tinha versões incompatíveis dos pacotes de push notification:
    - `laravel-notification-channels/apn: ^2.0` requeria PHP ^7.2 (incompatível com PHP 8.5). Atualizado para `^6.0`.
    - `laravel-notification-channels/fcm: ^5.4` não existia (série 5.x vai até 5.1.0). Atualizado para `^6.0`.
  - **CRÍTICO #2**: Pacotes APN/FCM não estavam no `composer.lock` (composer.json editado manualmente sem `composer update`). Rodado `composer update` para gerar lock e instalar dependências.
  - **CRÍTICO #3**: Testes travavam/timeout por deadlocks no PostgreSQL causados pelo paratest (Pest 4.x) criando bancos temporários e rodando `LazilyRefreshDatabase` em paralelo. Solução: rodar testes com flag `-p1` (single process) e criar banco temporário `agentflix_test_test_1` a partir do template.
  - **Resultado**: Todos os testes de gateway/push passam:
    - `GatewayBroadcastServiceTest`: 9 passed (14 assertions)
    - `DeviceTokenRegistrationTest`: 4 passed (17 assertions)
    - `NewMessageNotificationTest`: 4 passed (7 assertions)
  - Arquivos: `api/composer.json`, `api/composer.lock`
  - Ref: FEAT-047 / TASK-047.10, TASK-047.11
