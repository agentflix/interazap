# Changelog — 2026-05-05

> Registro factual do que mudou. Para decisões e contexto, veja MEMORY.

## Entradas

- [10:07] FIX [app]: Tradução do banner de status offline para pt-BR
  - Mensagem alterada para "Você está offline. Alguns recursos podem indisponíveis."
  - CTA alterado para "Tentar novamente"
  - Teste adicionado para garantir a renderização do texto traduzido
  - Arquivos: `app/src/app/shared/components/offline-banner/offline-banner.component.html`, `app/src/app/shared/components/offline-banner/offline-banner.component.spec.ts`
  - Ref: ajuste solicitado em conversa

- [18:40] REFACTOR [api, app]: Remoção do campo `mandatory_rules` não utilizado
  - Campo existia na UI, API e banco mas nunca era consumido na resolução do prompt de IA
  - Removido do frontend: form, model interface e payload
  - Removido do backend: Model, DTO, Request, Resource
  - Criada migration para remover coluna do banco
  - Atualizados factory e seeder
  - Testes do PlanPrompt passing (7 testes)
  - Arquivos:
    - `app/src/app/pages/platform/ai-governance/prompt-plans/prompt-plans.html`
    - `app/src/app/pages/platform/ai-governance/prompt-plans/prompt-plans.ts`
    - `app/src/app/pages/ai/models/ai.model.ts`
    - `api/src/Domain/Ai/Models/AiPromptPlan.php`
    - `api/src/Domain/Ai/DTOs/PlanPromptDTO.php`
    - `api/src/Domain/Ai/Http/Requests/UpdatePlanPromptRequest.php`
    - `api/src/Domain/Ai/Http/Resources/PlanPromptResource.php`
    - `api/database/migrations/2026_05_05_000001_remove_mandatory_rules_from_ai_prompt_plans.php`
    - `api/database/factories/AiPromptPlanFactory.php`
    - `api/database/seeders/AiPromptPlanSeeder.php`

- [16:26] FEAT [api, app, db]: FEAT-003 migra token limits para planos da plataforma e adiciona cobrança de overage
  - Adicionadas colunas `token_limit_monthly`, `allow_overage` e `overage_price_per_1k` em `platform_plans` com data migration a partir de `ai_prompt_plans`
  - Removidos os campos de token de `AiPromptPlan`, DTO, request, resource, factory, seeder e tela de prompt plans
  - Criados `CalculateAiOverageAction` e comando `billing:generate-monthly-invoices` com `--dry-run`, idempotência por `tenant_id + reference_month` e metadata de breakdown
  - Schedule mensal registrado para dia 1 às 06:00
  - Tela de edição de planos recebeu seção "Limite de IA"
  - Testes Pest adicionados para cálculo, dry-run, overage, idempotência e fatura sem excedente
  - Validação: testes focados passaram; `pnpm build` em `app/` passou; `composer analyse` global segue bloqueado por 88 erros preexistentes fora da FEAT-003
  - Ref: FEAT-003 / TASK-3.x / TASK-5.x

- [21:16] FIX [gateway, api]: Corrige loop de qualificação da Sofia com histórico conversacional estruturado
  - `MessageBuilderService` passou a expandir `conversation_history` em mensagens reais (`user`/`assistant`) quando `request.messages` está vazio
  - `conversation_history` foi removido do `context:` serializado para evitar duplicação no prompt
  - Adicionada deduplicação do `inputText` quando ele já é igual ao último turno de usuário no histórico
  - Seeder da Sofia atualizado com gatilhos adicionais de vendas e qualificação para delegação imediata
  - Arquivos: `gateway/src/domains/ai/services/orchestration/message-builder.service.ts`, `gateway/src/domains/ai/services/orchestration/message-builder.service.spec.ts`, `api/database/seeders/InteraZapProductAgentsSeeder.php`
  - Validação: `pnpm --filter gateway test -- src/domains/ai/services/orchestration/message-builder.service.spec.ts` (passou), `php artisan db:seed --class=InteraZapProductAgentsSeeder` (passou); suíte completa do gateway com 1 falha preexistente/flaky em `send-message.service.spec.ts` (assert de timing 9ms vs >=10ms)
  - Ref: ajuste solicitado em conversa
- [21:31] FIX [app/api]: Corrigido travamento do campo de contato no formulário de negociações ao selecionar empresa
  - Formulário agora busca contatos por empresa via API (`company_id`) e valida `contact_id` contra retorno atual.
  - Ajustado fallback de vínculo da empresa no contato para `crm_company_id` -> `company.id` -> `company_id`.
  - Backend passou a aceitar/aplicar filtro `crm_company_id` (com alias `company_id`) na listagem de contatos.
  - Arquivos: `app/src/app/pages/crm/negotiations/components/negotiation-form/negotiation-form.ts`, `app/src/app/core/services/contact.service.ts`, `app/src/app/core/models/contact.model.ts`, `api/src/Domain/CRM/Actions/CRMContactActions.php`, `api/src/Domain/CRM/Http/Controllers/CRMContactController.php`
  - Ref: BUG CRM negociação contato por empresa

- [21:31] TEST [app/api]: Adicionada cobertura de regressão para filtro de contatos por empresa
  - Frontend: specs do `NegotiationFormComponent` validando carga reativa por empresa e fallback `company.id`.
  - Backend: testes unit/feature cobrindo filtro `crm_company_id` na listagem de contatos.
  - Arquivos: `app/src/app/pages/crm/negotiations/components/negotiation-form/negotiation-form.spec.ts`, `api/tests/Unit/CRM/CrmContactActionsTest.php`, `api/tests/Feature/Crm/CrmContactTest.php`
  - Ref: BUG CRM negociação contato por empresa

- [21:42] DOCS [repo]: Criada skill `canonical-crm-flow` para otimizar correções de contrato canônico no CRM
  - Skill inclui cache de contexto (`context-map`, `test-map`, `decision-log`), fluxo incremental de testes e checklist PREVC.
  - Arquivo: `.claude/skills/canonical-crm-flow/SKILL.md`
  - Ref: Padronização de fluxo para bugfix/refactor CRM

- [21:48] DOCS [repo]: Criada skill global `flow-optimizer` e comando `/optimize-flow` para acelerar execução técnica
  - Skill transversal para todo o monorepo com fluxo balanceado: intake, smart search, impact map, test slice, guardrails e close loop.
  - Cache operacional padronizado em `.claude/agent-memory/flow-optimizer/`.
  - Comando `/optimize-flow` adiciona roteiro reutilizável para bugfix/refactor/feature/investigação.
  - Arquivos: `.claude/skills/flow-optimizer/SKILL.md`, `.claude/commands/optimize-flow.md`
  - Ref: otimização global de fluxo de trabalho

- [23:30] FEAT [api, app]: Flag `track_stock` exposta no cadastro de produto para controle de estoque
  - Backend: corrigido `shouldTrackStock()` em `CRMNegotiationProductActions` para respeitar **apenas** a flag `track_stock` (removido o `|| $product->stock > 0` que ignorava a intenção do usuário)
  - Backend: adicionado filtro por `track_stock` no `CRMProductActions::list()` e `listAll()`
  - Backend: adicionados 2 testes Pest — produto infinito não debita estoque e update não ajusta estoque quando `track_stock = false`
  - Frontend (form): adicionado switch "Controlar estoque" visível apenas para tipo Produto; removida auto-definição `track_stock = isProduct`
  - Frontend (listagem): adicionada coluna "Estoque" com badges "Controlado" (azul) / "Ilimitado" (emerald) / "N/A" (cinza para serviços)
  - Frontend (detalhes): adicionada seção "Controle de estoque" no painel lateral
  - Frontend (negociação): catálogo exibe info de estoque no select; badge de disponibilidade; quantity respeita `max` do estoque; componente `TextInput` ganhou suporte a `max` input
  - Interfaces TypeScript: `track_stock` e `stock` adicionados em `ProductService` (ambos os services: `crm-product-service.service.ts` e `product-service.service.ts`)
  - Validação: 26 testes backend relevantes passaram; `npx ng build` no `app/` passou
  - Arquivos:
    - `api/src/Domain/CRM/Actions/CRMNegotiationProductActions.php`
    - `api/src/Domain/CRM/Actions/CRMProductActions.php`
    - `api/src/Domain/CRM/Http/Controllers/CRMProductController.php`
    - `api/tests/Feature/CrmNegotiationProductsTest.php`
    - `app/src/app/pages/crm/products-services/components/product-service-form/crm-product-service-form.ts`
    - `app/src/app/pages/crm/products-services/components/product-service-form/crm-product-service-form.html`
    - `app/src/app/pages/crm/products-services/crm-products-services.html`
    - `app/src/app/pages/crm/negotiation-show/components/negotiation-products-tab/negotiation-products-tab.ts`
    - `app/src/app/pages/crm/negotiation-show/components/negotiation-products-tab/negotiation-products-tab.html`
    - `app/src/app/core/services/crm-product-service.service.ts`
    - `app/src/app/core/services/product-service.service.ts`
    - `app/src/app/shared/components/text-input/text-input.ts`
    - `app/src/app/shared/components/text-input/text-input.html`
  - Layout do formulário refatorado seguindo padrão das Configurações do Chat: switch "Controlar estoque" agora vem ANTES dos campos, que aparecem indentados com borda lateral (`border-l-2 border-accent-500/30`) apenas quando ativo. Quando inativo, exibe banner explicativo "produto ilimitado".
  - Campo "Unidade" trocado de text input para select com 16 unidades comuns (un, pc, cx, kg, g, lt, ml, m, m², m³, par, kit, conj, rol, sac, emb).
  - Campos "Quantidade em estoque" e "Estoque mínimo" trocados de `af-text-input` para `af-number-input` (UI kit com botões +/− de incremento/decremento).
  - Badges da listagem: colunas "Tipo" e "Estoque" agora usam `<af-status-badge>` seguindo o mesmo padrão da coluna "Status" (Ativo/Inativo) — indicador visual uniforme com dot colorido ao invés de pills com background.
  - Ref: feature flag controle de estoque em produtos CRM
