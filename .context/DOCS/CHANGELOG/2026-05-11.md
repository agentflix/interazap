# Changelog — 2026-05-11

> Registro factual do que mudou. Para decisões e contexto, veja MEMORY.

---

## Entradas

- [14:30] TEST [gateway]: Corrigir 47 erros de typecheck em specs
  - Alinhar mocks com contratos TS atuais (SendResult, NormalizedUazapiEvent, ConnectInstanceDto, GatewayMessage, GatewayErrorCode)
  - Corrigir handshake readonly em events.gateway.spec.ts via `(mockSocket as any).handshake`
  - Corrigir mockedAxios casting em webhook-dispatcher.service.spec.ts
  - Corrigir databaseService não declarado em health.service.coverage.spec.ts
  - Corrigir provider type inference em chat-webhook.service.spec.ts (`as const`)
  - Corrigir normalizeWebhook mockReturnValue → mockResolvedValue (interface retorna Promise)
  - Arquivos: `gateway/src/**/*.spec.ts` (10 arquivos)
  - Ref: TASK-GW-TEST-001

- [14:45] TEST [gateway]: Adicionar typecheck de specs no build
  - Criar `gateway/tsconfig.spec.json` incluindo specs no typecheck
  - Adicionar script `typecheck:specs` em `gateway/package.json`
  - Garante que specs não driftam dos contratos TS
  - Arquivos: `gateway/tsconfig.spec.json`, `gateway/package.json`
  - Ref: TASK-GW-TEST-001

- [15:00] TEST [api]: Remover dump() de testes N+1
  - Substituir `dump()` por `info()` com guard `env('DEBUG_N_PLUS1')`
  - Testes não geram mais ruído no output
  - Arquivos: `api/tests/Feature/Domain/CRM/CRMNegotiationN+1Test.php`, `api/tests/Feature/Domain/CRM/CRMContactN+1Test.php`
  - Ref: TASK-API-TEST-001

- [15:10] TEST [api]: Reativar teste rate limit do QueueHealthController
  - Remover `->skip()` do teste de rate limiting
  - Usar `Cache::flush()` para isolamento de estado
  - QueueHealthService é final — testes que precisavam de mock agora usam service real
  - Arquivos: `api/tests/Feature/Platform/QueueHealthControllerTest.php`
  - Ref: TASK-API-TEST-001

- [16:00] FEAT [api, gateway]: Auditoria e segurança em admin de filas + CORS Telegram
  - QueueAdminController: injetar AuditLogger, auditar todas as ações mutantes (pause, resume, clean, DLQ retry/purge, circuit reset/open)
  - Criar QueueAdminPolicy com gate `platform.queues.manage`
  - Corrigir ordem de rotas: estáticas (/dlq, /circuits) antes de dinâmicas (/{name}) para evitar route collision
  - Gateway Telegram WebSocket: trocar CORS `origin: '*'` por allowlist via `TELEGRAM_WS_ALLOWED_ORIGINS` (fallback: localhost em dev)
  - Validar .env.bak: não versionado atualmente, mas já esteve no histórico git — recomendar rotação de segredos em task separada
  - App QueueService: já alinhado com contrato API, sem alterações necessárias
  - Arquivos: `api/src/Domain/Platform/Http/Controllers/QueueAdminController.php`, `api/src/Domain/Platform/Policies/QueueAdminPolicy.php`, `api/src/Domain/Platform/Routes/platform.php`, `api/app/Providers/AuthServiceProvider.php`, `api/tests/Feature/Platform/QueueAdminControllerTest.php`, `gateway/src/bot/gateways/telegram-websocket.gateway.ts`, `gateway/src/bot/gateways/telegram-websocket.gateway.spec.ts`
  - Ref: FEAT-005
