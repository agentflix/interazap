# Changelog — 2026-04-20

> Registro factual do que mudou. Para decisões e contexto, veja MEMORY.

## Formato

Cada entrada segue:

```text
- [HORA] [TIPO] [ESCOPO]: Descrição concisa
  - Detalhes relevantes
  - Arquivos principais afetados
  - Task/Feature relacionada
```

## Tipos

- `FEAT` — Nova funcionalidade
- `FIX` — Correção de bug
- `REFACTOR` — Refatoração sem mudança de comportamento
- `DOCS` — Documentação
- `TEST` — Testes
- `CHORE` — Configuração, tooling, infra
- `BREAKING` — Mudança que quebra compatibilidade

---

## Entradas

- [14:00] FEAT [landing]: Adição de ícone de chat e telemetria na Landing Page
    - Implementação de um botão fixo inferior direito apontando para o chat externo, com contrato de dados `data-iz-*`, acessibilidade completa e despacho de eventos `iz:chat_launcher_*`.
    - Arquivos: `landing/index.html`
    - Ref: FEAT-041 / TASK-041.3

- [23:55] REFACTOR [Arch]: Correções de debt de arquitetura (Angular Leaks, NestJS Schema e Laravel Raws)
    - Angular: Adicionado pipe `takeUntilDestroyed(this.destroyRef)` no `auth-store.service.ts` para fechar loose subscription de ciclo infinito.
    - NestJS: Adicionado pipeline de estrita validação `MetaWebhookDto` em `meta-webhook.controller.ts` para `phone_number_id`, eliminando `optional chaining silencioso` com base no `payload?.entry`.
    - Laravel: Isolação de lógica analítica e particionamento `Window SQL functions (ROW_NUMBER)` da `ListChatTicketsAction` e do gerador M5 `SendMessageAction` para os scopes limpos (`scopeWithContactDeduplicationRank`, `scopeWithStatusCounts`, `scopeWhereContentHash`).
    - Arquivos Relevantes: `auth-store.service.ts`, `meta-webhook.controller.ts`, `ChatTicket.php`, `ChatMessage.php` e suas Actions associadas.
    - Status: Segredos e `.env` reportados e assumidos pela equipe DevSecOps para rotação na infra, desconsiderados do histórico local de dev.
