# Changelog — 2026-04-21

> Registro factual do que mudou. Para decisões e contexto, veja MEMORY.

## Formato

Cada entrada segue:

```text
- [HORA] [TIPO] [ESCOPO]: Descrição concisa
  - Detalhes relevantes
  - Arquivos principais afetados
  - Task/Feature relacionada
```

## Tipos

- `FEAT` — Nova funcionalidade
- `FIX` — Correção de bug
- `REFACTOR` — Refatoração sem mudança de comportamento
- `DOCS` — Documentação
- `TEST` — Testes
- `CHORE` — Configuração, tooling, infra
- `BREAKING` — Mudança que quebra compatibilidade

---

## Entradas

- [14:10] FIX [gateway-logger]: Restaurado logger colorido no desenvolvimento e controle de verbosidade por ambiente
    - `main.ts` passou a selecionar logger estruturado JSON apenas quando `LOG_STRUCTURED=true` (padrão em produção) e logger padrão colorido no dev.
    - Adicionado suporte a `LOG_LEVEL` para filtrar saída no console local.
    - Arquivos: `gateway/src/main.ts`, `gateway/.env.example`, `gateway/README.md`
    - Ref: FEAT-041 / DEBUG-LOGS

- [14:12] FIX [realtime-auth]: Redução de ruído de autenticação WebSocket no fallback JWT → Sanctum
    - Log esperado de fallback foi reduzido de `debug` para `verbose` em `WsAuthenticationService`.
    - Teste ajustado para mockar método `verbose` do logger.
    - Arquivos: `gateway/src/domains/realtime/services/ws-authentication.service.ts`, `gateway/src/domains/realtime/services/ws-authentication.service.spec.ts`
    - Ref: FEAT-041 / DEBUG-LOGS

- [17:30] FEAT [chat/webchat]: Encerramento do ticket iniciado pelo cliente no webchat externo (FEAT-042)
    - Adicionado endpoint público `POST /api/webchat/close` sem autenticação Sanctum, validado por JWT da sessão webchat.
    - `UpdateChatTicketAction` agora publica evento `ticket.updated` com `event_type: 'ticket_closed'` quando fechamento ocorre via fluxo público.
    - Frontend: `webchat.service.ts` estendido com sinais `ticketStatus`, `isClosing`, `isClosed` e método `closeTicket()`.
    - Frontend: `chat-window.component` recebeu botão "Encerrar chamado" no header, modal de confirmação e estado final bloqueando composer.
    - Tela interna do atendente sincronizada via `ticket.updated` (já tratado pelo `chat-store`).
    - Arquivos backend: `WebChatCloseController.php` (novo), `webchat.php` (rota), `UpdateChatTicketAction.php` (broadcast), `WebChatCloseControllerTest.php` (novo), `UpdateChatTicketActionTest.php` (estendido)
    - Arquivos frontend: `webchat.model.ts`, `webchat.service.ts`, `webchat.service.spec.ts`, `chat-window.component.ts`, `chat-window.component.html`, `chat-window.component.spec.ts`, `chat-store.spec.ts`
    - Testes: 57 testes passando (4 feature + 2 unit backend, 16 service + 26 component + 9 store frontend)
    - Ref: FEAT-042

- [19:45] FIX [webchat/realtime]: Correção de mensagens em tempo real no webchat público (FEAT-043)
    - 4 bugs corrigidos no pipeline WebSocket realtime:
        1. `WebChatMessageController` não emitia broadcast após mensagem do contato (agente não via) — adicionado `ProcessChatMessageAction` e chamada `emitNewMessageEvent`
        2. `SendChatMessageAction` não emitia para mensagens outgoing do agente — adicionado condição para `SOURCE_AGENT`
        3. Event name mismatch: `webchat.ai_response` (dot) vs `webchat:ai_response` (colon) — corrigido em `WebChatRedisPublisher.php`
        4. Não havia gateway handler para namespace `/webchat` — criado `WebChatGateway` com handler `webchat:join`
    - `EventFanoutService` agora processa `webchat:ai_response` e emite via `WebChatGateway.emitToRoom` para `session:{sessionId}`
    - `JwtPayload` estendido com `session_id` opcional para suportar tokens de sessão webchat
    - Backend: `WebChatMessageController.php`, `SendChatMessageAction.php`, `WebChatRedisPublisher.php`, `webchat.gateway.ts` (novo), `event-fanout.service.ts`
    - Frontend: validação de listener em `webchat.service.ts` (já usava `webchat:ai_response` correto)
    - Testes: 27 testes EventFanoutService passando
    - Ref: FEAT-043

    - [21:05] FIX [webchat/ui]: Corrigida quebra prematura de texto em mensagens sem Enter no chat externo
        - Linha de mensagem no `chat-window` passou a usar largura total (`w-full`) para que o limite do balão (`max-w-[85%]`) seja calculado corretamente no container.
        - `af-chat-bubble` também foi ajustado para `width: 100%` no wrapper, mantendo `max-w-[85%]` no próprio balão.
        - Corrigido efeito de quebra visual em duas linhas em mensagens de uma linha por cálculo shrink-to-fit da linha de mensagem.
        - Adicionado teste de regressão para garantir que as linhas de mensagem renderizam com largura total.
        - Arquivos: `app/src/app/pages/webchat/components/chat-window/chat-window.component.html`, `app/src/app/shared/components/chat-bubble/chat-bubble.ts`, `app/src/app/pages/webchat/components/chat-window/chat-window.component.spec.ts`
        - Testes: `npm run test:run -- --include src/app/pages/webchat/components/chat-window/chat-window.component.spec.ts` (27 passando)
        - Ref: DEBUG-WEBCHAT-WRAP

- [22:04] FIX [webchat]: Correção de 3 bugs no chat externo (encerramento, upload de arquivo, preview de última mensagem)
    1. **Encerramento (cliente):** quando atendente encerra o chamado, a tela do cliente agora retorna automaticamente à PreChat após 2.5s (antes ficava parada exigindo clique manual em "Iniciar novo chamado"). Subscription de `ticketClosedByAgent$` em `chat-window` agora dispara `setTimeout(() => ticketClosed.emit(), 2500)`.
    2. **Upload de arquivo:** `WebChatMessageController` agora persiste `file_name` da mídia (via `ChatMessageExtended`) e `WebChatMessageRequest` aceita o campo. Frontend `sendFileMessage` propaga `upload.file_name` para o backend. Sem `file_name` o atendente caía no fallback de imagem do `chat-message-media`.
    3. **Preview "Sem mensagem":** dois ajustes: (a) `WebChatMessageController` agora atualiza `last_message_at` do ticket após cada mensagem, garantindo ordenação e atualização da lista do atendente; (b) `WebChatSessionController` injeta `ChatActivityBroadcastService` e emite subevent `ticket.new` ao criar tickets webchat, tornando-os visíveis em tempo real na lista do atendente sem F5.
    - Arquivos backend: `api/src/Domain/Chat/Http/Controllers/WebChatMessageController.php`, `api/src/Domain/Chat/Http/Controllers/WebChatSessionController.php`
    - Arquivos frontend: `app/src/app/pages/webchat/components/chat-window/chat-window.component.ts`, `app/src/app/pages/webchat/services/webchat.service.ts`, `app/src/app/pages/webchat/webchat.model.ts`
    - Validação: `tsc --noEmit -p tsconfig.app.json` sem erros no escopo webchat/chat; `php -l` sem erros nos controllers.
    - Ref: DEBUG-WEBCHAT-3BUGS

- [22:55] FIX [chat/realtime]: Listagem de tickets do `/chat` passa a reagir a criação de ticket via websocket sem F5
    - `Chat` agora escuta `chat.activity` (subevent `ticket.new`) e `chat.ticket.new`, acionando `ChatRefreshService.request()` com cooldown curto para evitar rajadas.
    - Corrigido desalinhamento entre payload realtime atual (chat.activity/ticket.new) e listener legado (`message.received`) da tela principal do chat.
    - Teste unitário adicionado para garantir refresh da lista ao receber `ticket.new` no envelope `chat.activity`.
    - Arquivos: `app/src/app/pages/chat/chat.ts`, `app/src/app/pages/chat/chat.spec.ts`
    - Testes: `pnpm --dir app exec ng test --watch=false --include src/app/pages/chat/chat.spec.ts` (5 passando)
    - Ref: DEBUG-CHAT-EXTERNAL-TICKET-LIST
