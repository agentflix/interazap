# Changelog — 2026-05-01

> Registro factual do que mudou. Para decisões e contexto, veja MEMORY.

---

## Entradas

- [12:32] TEST Autopilot: Trilha E2E completa do módulo Autopilot — 92/92 PASS
  - 29 tools validadas com dados reais (PostgreSQL local, sem mocks)
  - Matriz de permissões: 11 cenários (8 bloqueios + 6 liberações)
  - ToolDispatcherService, AutopilotRunSnapshotResolver e AiAutopilotRun validados
  - Scripts: `api/tests/E2E/Autopilot/` (16 arquivos) + `api/tests/E2E/run-e2e.sh`
  - Evidência completa: `.context/DOCS/CHANGELOG/2026-05-01-e2e-autopilot-evidence.md`

- [03:00] FIX AI: Corrige falha permanente do AiKnowledgeProcessJob por coluna tenant_id inexistente
  - Causa raiz: modelo `AiKnowledgeChunkRef` usa `BelongsToTenant` (aplica `TenantScope` global), mas a migration que criou `ai_knowledge_chunk_refs` não incluiu `tenant_id`. Toda operação Eloquent (ex: `delete()`) injetava `WHERE tenant_id = ?` em uma coluna inexistente, gerando `SQLSTATE[42703]`.
  - Migration criada: `database/migrations/2026_05_01_030000_add_tenant_id_to_ai_knowledge_chunk_refs.php`
  - Modelo atualizado: `api/src/Domain/Ai/Models/AiKnowledgeChunkRef.php` (tenant_id no $fillable)
  - Job atualizado: `api/src/Domain/Ai/Jobs/AiKnowledgeProcessJob.php` (inclui tenant_id no INSERT raw de refs)
  - Teste atualizado: `api/tests/Feature/Domain/Ai/Jobs/AiKnowledgeProcessJobDedupTest.php`
  - Ref: FEAT-051 (RAG/deduplicação)
