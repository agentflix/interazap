# Changelog — 2026-05-01

> Registro factual do que mudou. Para decisões e contexto, veja MEMORY.

---

## Entradas

- [17:45] FEAT Chat: Sistema de auto-fechamento de tickets por inatividade (FEAT-053)
  - **Pré-requisitos (TASK-0.1, TASK-0.2):**
    - Migration `2026_05_01_100000`: colunas `last_customer_message_at`, `last_agent_message_at` em `chat_tickets` + índices
    - Listener `UpdateTicketActivityTimestampsListener`: atualiza timestamps por direção (incoming/outgoing)
    - `MessagePersisted`: refatorado para disparar em mensagens outgoing (antes só incoming)
    - `TicketClosedEvent`: parâmetro `$closedMode` adicionado ('auto_inactivity', 'normal', 'forced')
  - **Migrations (TASK-1.0):**
    - `2026_05_01_100001`: coluna `settings_chat` (JSONB) em `platform_tenants`
    - `2026_05_01_100002`: colunas `auto_close_enabled`, `auto_close_after_minutes`, `auto_close_target`, `auto_close_message` em `chat_instances`
  - **Modelos (TASK-1.1):**
    - `PlatformTenant`: cast array para `settings_chat`
    - `ChatInstance`: 4 colunas de auto-close no fillable + método `getEffectiveAutoCloseConfig()` com herança tenant→canal
  - **Lógica de negócio (TASK-1.2, TASK-1.3, TASK-1.4):**
    - `CloseInactiveTicketsAction`: batch SQL que agrupa por target (both/client/agent), fecha em transação, dispara `TicketClosedEvent` com `closedMode='auto_inactivity'`
    - `CloseInactiveTicketsCommand`: `chat:close-inactive-tickets` com opção `--tenant=`, agendado `everyFiveMinutes()` em `routes/console.php`
    - `UpdateChatTicketAction`: quando `mode='auto_inactivity'`, envia mensagem de auto-close em vez de `end_service_message`
    - `ChatTicketNotificationListener`: early-return para `closedMode='auto_inactivity'` (suprime notificações)
  - **API Endpoints (TASK-1.5, TASK-1.6):**
    - `PlatformTenantController`: GET/PATCH `/settings` aceita `settings_chat`
    - `UpdateTenantSettingsRequest`: validação dos 4 campos de auto-close (minutes: [5,10,15,30,45,60,120], target: [both,client,agent], message: max 2000)
    - `ChatInstanceController`: aceita campos de auto-close no update
    - `PlatformTenantResource` e `ChatInstanceResource`: incluem configurações de auto-close
  - **Frontend (TASK-2.0, TASK-2.1, TASK-2.2, TASK-2.3):**
    - `tenant-settings`: seção "Auto-fechamento de atendimentos" com toggle, select de tempo, radio de target, textarea com contador
    - `channel-form`: aba "Auto-fechamento" com toggle "Usar configuração global" e campos condicionais
    - Modelos: `TenantChatAutoCloseSettings` (tenant-settings.model.ts), `Integration.auto_close_*` (integration.model.ts)
    - Menu: sem alterações — config acessível via Configurações > Configurações do Inquilino
  - **Testes (TASK-3.0):** 16/16 testes Pest passando em `CloseInactiveTicketsTest.php`
  - **Bug corrigido:** early-return do `enabled` global na Action removido — agora permite override por canal
  - Ref: `docs/superpowers/specs/2026-05-01-auto-close-inatividade-design.md`

- [12:32] TEST Autopilot: Trilha E2E completa do módulo Autopilot — 92/92 PASS
  - 29 tools validadas com dados reais (PostgreSQL local, sem mocks)
  - Matriz de permissões: 11 cenários (8 bloqueios + 6 liberações)
  - ToolDispatcherService, AutopilotRunSnapshotResolver e AiAutopilotRun validados
  - Scripts: `api/tests/E2E/Autopilot/` (16 arquivos) + `api/tests/E2E/run-e2e.sh`
  - Evidência completa: `.context/DOCS/CHANGELOG/2026-05-01-e2e-autopilot-evidence.md`

- [03:00] FIX AI: Corrige falha permanente do AiKnowledgeProcessJob por coluna tenant_id inexistente
  - Causa raiz: modelo `AiKnowledgeChunkRef` usa `BelongsToTenant` (aplica `TenantScope` global), mas a migration que criou `ai_knowledge_chunk_refs` não incluiu `tenant_id`. Toda operação Eloquent (ex: `delete()`) injetava `WHERE tenant_id = ?` em uma coluna inexistente, gerando `SQLSTATE[42703]`.
  - Migration criada: `database/migrations/2026_05_01_030000_add_tenant_id_to_ai_knowledge_chunk_refs.php`
  - Modelo atualizado: `api/src/Domain/Ai/Models/AiKnowledgeChunkRef.php` (tenant_id no $fillable)
  - Job atualizado: `api/src/Domain/Ai/Jobs/AiKnowledgeProcessJob.php` (inclui tenant_id no INSERT raw de refs)
  - Teste atualizado: `api/tests/Feature/Domain/Ai/Jobs/AiKnowledgeProcessJobDedupTest.php`
  - Ref: FEAT-051 (RAG/deduplicação)

- [18:15] FEAT Chat: Cria `ChatRoutingQueuePolicy` para controle de acesso às filas de roteamento (FEAT-052 Fase 1)
  - Policy criada: `api/src/Domain/Chat/Policies/ChatRoutingQueuePolicy.php`
    - `view()` — requer permissão `chat.routing.view` + mesmo tenant
    - `manage()` — requer permissão `chat.routing.manage` + mesmo tenant (quando fila informada)
    - `addAgent()` — requer `chat.routing.manage` + validação cross-tenant do agente via `AuthUser::query()->where(tenant_id)`
  - Registro no `AuthServiceProvider`: `ChatRoutingQueue::class => ChatRoutingQueuePolicy::class`
  - Sintaxe validada (`php -l`) e policy resolvível via `Gate::getPolicyFor()`
  - Ref: FEAT-052 / TASK-052-04
