# Changelog — 2026-04-26

> Registro factual do que mudou. Para decisões e contexto, veja MEMORY.

---

## Entradas

- [18:30] FIX [gateway]: Adicionar campo `mode` opcional ao `ConnectInstanceDto`
    - `@IsOptional() @IsEnum(['qr', 'pair']) mode?: 'qr' | 'pair'` adicionado
    - Eliminado erro 400 "property mode should not exist" ao iniciar connect com modo QR
    - Arquivo: `gateway/src/domains/chat/dto/connect-instance.dto.ts`
    - Commit: `210f4c5`
    - Release deploy: `20260426205816`

- [20:00] FIX [infra]: Adicionar variáveis de webhook Uazapi ao heredoc do gateway no Ansible
    - `UAZAPI_WEBHOOK_URL`, `UAZAPI_WEBHOOK_EVENTS`, `UAZAPI_WEBHOOK_EXCLUDE_MESSAGES`, `UAZAPI_WEBHOOK_RETRIES` adicionados a `tasks/main.yml`
    - `vault.yml` re-criptografado com `uazapi_webhook_url: 'https://gateway.interazap.com.br'`
    - Arquivo: `infra/ansible/roles/app-deploy/tasks/main.yml`
    - Commit: `210f4c5`

- [21:33] FIX [infra]: Aumentar `GATEWAY_TIMEOUT` de 3s para 35s
    - Causa raiz: Uazapi `/instance/connect` leva ~13s para gerar QR; timeout de 3s causava `cURL error 28`
    - QR era gerado corretamente pela Uazapi mas nunca chegava à API Laravel
    - Arquivo: `infra/ansible/roles/app-deploy/tasks/main.yml` (linha 162)
    - Commit: `2a2b529`
    - Release deploy: `20260426213329`
    - **Validação ponta-a-ponta**: `POST /api/channels/{id}/connect` → `{"success":true, "status":"qr", qr_code: "data:image/png;base64,..."}` ✅

- [21:48] FIX [landing]: Corrigir `data-iz-tenant-id` na landing page
    - Valor era UUID inexistente `3453efd7-...` → corrigido para `a1a30625-3b6f-42b2-b6e9-ea8471192c7e`
    - Arquivo: `landing/index.html` (linha 2597)
    - Commit: `53de060`
    - Release deploy: `20260426214828`

- [19:12] FIX [gateway]: Adicionar `X-Session-ID` ao CORS `allowedHeaders` do NestJS gateway
    - Causa raiz: `traceIdInterceptor` Angular adiciona `X-Session-ID` em todas as requests; header não estava listado no CORS do gateway; browser recebia OPTIONS 204 mas silenciosamente rejeitava o preflight porque `X-Session-ID` não aparecia em `Access-Control-Allow-Headers` — POST nunca era enviado (erro de rede status 0 no Angular)
    - Sintoma: "Não foi possível iniciar a conversa" no webchat externo; logs nginx mostravam OPTIONS sem POST subsequente; curl direto funcionava (curl não faz preflight CORS)
    - Arquivo: `gateway/src/main.ts` (array `allowedHeaders` em `app.enableCors()`)
    - Commit: `c038dea`
    - Deploy: hotfix direto na release `20260426214828` + `pm2 restart interazap-gateway-prod`
    - **Validação**: `OPTIONS` retorna `access-control-allow-headers: ...,X-Session-ID,...` ✅; `POST` retorna 201 com token/sessionId ✅

- [22:33] FIX [webchat]: Corrigir namespace Socket.IO do WebChatService Angular
    - Causa raiz: `io(apiBase, { path: '/ws' })` conectava ao namespace raiz `/` (EventsGateway) em vez de `/webchat` (WebChatGateway); eventos `webchat:ai_response` e `webchat:agent_message` emitidos pelo `EventFanoutService` via `webChatGateway.emitToRoom()` vivem no namespace `/webchat` — cliente no namespace errado nunca os recebia
    - Fix: `gatewayUrl = \`${apiBase}/webchat\``para que Socket.IO conecte ao namespace`/webchat`; também corrigida constante `WEBCHAT_PATH`de`/ws/webchat`(errada) para`/ws`
    - Arquivo: `app/src/app/pages/webchat/services/webchat.service.ts` (linha 19 e 274)
    - Commit: `309ee42`
    - Release deploy: `20260426223353`
    - **Validação**: Socket.IO handshake `GET /ws/socket.io/?EIO=4&transport=polling` retorna `200` com sid ✅; gateway online, health check `/health` → `OK` ✅

- [23:55] FEAT [app-mobile]: Fase 4 FEAT-047 — TASK-047.7, TASK-047.8, TASK-047.9 concluídas
    - **TASK-047.7** (Plugins UI): `NativeBridgeService` (status bar teal, splash hide, keyboard resize, haptics, app lifecycle), `BackButtonService`, `PlatformService` — todos implementados com specs Vitest. WS reconnect wired: `app.ts` chama `realtime.connect()` em `onAppResume`.
    - **TASK-047.8** (Safe areas): `viewport-fit=cover` em `index.html`, variáveis CSS `--safe-area-*` + utilities `.pt-safe/.pb-safe/.pl-safe/.pr-safe` em `styles.css`, `topbar.html` já tinha `pt-safe pl-safe pr-safe`, `footer.html` recebeu `pb-safe pl-safe pr-safe`.
    - **TASK-047.9** (Back button Android): `BackButtonService` com lógica modal→history→exit, `registerModalCloser()`, no-op em web/iOS.
    - Arquivos modificados: `app/src/app/app.ts`, `app/src/app/layout/components/footer/footer.html`, `app/src/app/core/services/platform/native-bridge.service.ts`
    - 0 erros de TypeScript | Sem regressão nos gates de escopo
