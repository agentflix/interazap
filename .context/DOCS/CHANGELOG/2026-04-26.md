# Changelog — 2026-04-26

> Registro factual do que mudou. Para decisões e contexto, veja MEMORY.

---

## Entradas

- [00:35] FEAT [Platform]: Endpoint público de captação de leads (`POST /api/public/leads`)
  - Novo bounded context `Domain/Platform` ganha entidade `PlatformLead` (lead do produto InteraZap, sem `tenant_id`).
  - Endpoint público (sem `auth:sanctum`) sob `throttle:public` (30 req/min/IP).
  - Validação BR de telefone (com/sem máscara, normaliza para dígitos), e-mail RFC, consentimento LGPD obrigatório.
  - Honeypot anti-bot via campo `website` (silent drop, retorna 201 fake).
  - Deduplicação 24h por `email` OU `phone` na mesma `source` (faz `touch` no existente).
  - Resposta NÃO expõe `email`/`phone` (proteção LGPD).
  - Evento `PlatformLeadCreated` disparado para futuros listeners (CRM/notificação comercial).
  - 13 testes Pest, 47 assertions, todos verdes. Pint + PHPStan limpos no escopo.
  - Arquivos: `api/database/migrations/2026_04_26_003227_create_platform_leads_table.php`, `api/src/Domain/Platform/{Models,DTOs,Actions,Events,Http/{Requests,Resources,Controllers},Routes}/`, `api/tests/Feature/Platform/PlatformLeadStoreTest.php`, `api/routes/api.php`.
  - Ref: feature de redesign da landing (sessão de brainstorm 42695-1777174136).

- [00:55] FEAT [Landing]: Refactor completo do corpo + modal exit-intent + integração de leads
  - Direção visual aprovada: **opção C — híbrida** (clareza + conversão, sem comparativos com concorrente).
  - Header preservado (linhas originais 168-236) + chat launcher FEAT-041 intocado.
  - **Removido:** Seção 4 (Social Proof falsa), badge App Store, "Vagas limitadas para onboarding", "primeiro CRM brasileiro com Autopilot de IA nativo", CNPJ fake do footer, "sem cartão de crédito".
  - **Reescrito:** Hero, Problema, 3 Pilares (`#solucao`), Recursos (`#features`), Como Funciona (`#como-funciona`), App Mobile (só Google Play), Formulário (`#contato`), CTA Final, Footer.
  - **Novo:** modal exit-intent com `role=dialog`, `aria-modal`, focus trap, fecha com Esc/click fora, cooldown 1x/sessão (`sessionStorage.iz_exit_modal_shown`), gating se lead já enviado (`localStorage.iz_lead_submitted`) ou se < 8s na página.
  - **Triggers exit-intent:** desktop `mouseleave clientY≤0`; mobile `touchmove` rápido para cima + `visibilitychange` + `pagehide`.
  - **Integração:** form principal e modal chamam `POST /api/public/leads` via `INTERAZAP_API_URL` (configurável por `window.INTERAZAP_API_URL`, default `https://api.interazap.com.br`).
  - Coleta automática de UTMs, `referrer`, `user_agent`. Honeypot `website`.
  - Tratamento 422 / 429 / 5xx com mensagens amigáveis.
  - SEO estrutural: title/meta description honestas, OG tags, JSON-LD `Organization`, hierarquia H1→H2→H3.
  - Arquivos: `landing/index.html`.
  - Ref: feature de redesign da landing.

- [00:56] DOCS [Platform/Landing]: Memórias e changelog do dia
  - Arquivos: `.context/DOCS/CHANGELOG/2026-04-26.md`, `.context/DOCS/MEMORY/2026-04-26-public-leads-endpoint.md`.

- [16:18] FEAT [Desktop/Electron]: Build inicial dos executáveis InteraZap Desktop (.dmg + .exe)
  - Gerado ícone do projeto (raio InteraZap, teal `#14b8a6`) a partir de `landing/favicon.svg`:
    - `electron/build/icon.png` (1024×1024)
    - `electron/build/icon.icns` (macOS multi-resolução, via `sips` + `iconutil`)
    - `electron/build/icon.ico` (Windows multi-res 16/32/48/64/128/256, via `png-to-ico`)
    - `electron/build/icons/*.png` (Linux 16→1024)
  - Workspace pnpm passou a incluir `electron/` (`pnpm-workspace.yaml`).
  - `electron-builder.yml` ajustado: `compression: maximum`, `identity: null` (sem code signing macOS), `artifactName` dedicado por arch, inclui `app/**/*` (renderer Angular copiado).
  - `electron/package.json` ganhou pipeline `prebuild` (build:angular → copy:renderer → build:main) e scripts pnpm.
  - Renderer Angular copiado de `app/dist/app-new/browser/` → `electron/app/browser/` (29MB).
  - Artefatos gerados em `electron/release/`:
    - `InteraZap Desktop-1.0.0-arm64.dmg` (88MB)
    - `InteraZap Desktop-1.0.0-x64.dmg` (92MB)
    - `InteraZap Desktop Setup 1.0.0.exe` (81MB, NSIS x64, sem assinatura)
  - Build Windows feito direto no macOS (electron-builder baixou Wine 4.0.1 portátil automaticamente).
  - Validado: DMG arm64 monta, contém `InteraZap Desktop.app` + atalho `Applications`.
  - Arquivos principais: `electron/electron-builder.yml`, `electron/package.json`, `electron/build/icon.{png,icns,ico}`, `pnpm-workspace.yaml`.
  - Ref: solicitação de build de executável desktop.
