# Changelog — 2026-05-10

- [00:21] FIX [api]: Bloqueio de IA no WebChat para tenants sem `ai_enabled`
  - `WebChatMessageController` passou a consultar `PlatformPlanEnforcementService::isAiEnabled()` antes de disparar `ChatAutopilotResponder`.
  - `AiGateKeeperListener` agora aplica gate de plano (além do gate de atendimento humano), impedindo dispatch de `AutopilotTriggerFired` quando IA está desabilitada no plano.
  - Corrigidos fallbacks de IA para `false` quando plano atual não está disponível em `BillingSubscriptionController` e `PlatformTenantDetailsResource`.
  - Arquivos: `api/src/Domain/Chat/Http/Controllers/WebChatMessageController.php`, `api/src/Domain/Ai/Listeners/AiGateKeeperListener.php`, `api/src/Domain/Billing/Http/Controllers/BillingSubscriptionController.php`, `api/src/Domain/Platform/Http/Resources/PlatformTenantDetailsResource.php`
  - Ref: hotfix de enforcement de plano de IA (WebChat + Gatekeeper)

- [00:21] TEST [api]: Cobertura de regressão para bypass de IA por plano
  - `WebChatMessageControllerTest` cobre os dois fluxos: dispara IA com plano habilitado e não dispara com plano desabilitado.
  - Criado `AiGateKeeperListenerTest` cobrindo dispatch permitido, bloqueio por `human_takeover_at` e bloqueio por plano sem IA.
  - `BillingSubscriptionTest` valida fallback de `usage.ai.enabled=false` quando plano corrente fica indisponível (soft delete).
  - `PlatformTenantDetailsTest` ajustado para validar `data.resources.ai.enabled=false` sem plano ativo.
  - Arquivos: `api/tests/Feature/Chat/WebChatMessageControllerTest.php`, `api/tests/Unit/Ai/AiGateKeeperListenerTest.php`, `api/tests/Feature/BillingSubscriptionTest.php`, `api/tests/Feature/PlatformTenantDetailsTest.php`
  - Ref: hotfix de enforcement de plano de IA (WebChat + Gatekeeper)

- [12:31] FIX [api]: Fallback de WebChat para auto-reply quando plano não permite IA
  - `WebChatMessageController` passou a espelhar o roteamento do WhatsApp: texto com `ai_enabled=true` segue para Autopilot; texto com `ai_enabled=false` agora dispara `ChatAutoReplyResponder` em vez de silêncio.
  - Cálculo de `isFirstInteraction` no WebChat usa contagem de mensagens de contato após persistência (`count() === 1`) para habilitar boas-vindas na primeira mensagem.
  - Arquivos: `api/src/Domain/Chat/Http/Controllers/WebChatMessageController.php`
  - Ref: hotfix de fallback WebChat sem IA

- [12:31] TEST [api]: Regressão de fallback WebChat IA → auto-reply
  - `WebChatMessageControllerTest` passou a validar que, em plano sem IA, não há `AiRunRequested` e há dispatch de `ChatAutoReplyRespondJob` com `isFirstInteraction=true`.
  - Também valida ausência de dispatch do auto-reply quando o plano permite IA.
  - Arquivos: `api/tests/Feature/Chat/WebChatMessageControllerTest.php`
  - Ref: hotfix de fallback WebChat sem IA

- [12:57] FIX [api]: Supervisor dedicado para fila `auto-reply` no Horizon
  - Adicionado `supervisor-auto-reply` em `config/horizon.php` para consumir continuamente a fila `auto-reply` (chatbot/menu WebChat), evitando backlog e silêncio para tenants sem IA.
  - Mantido isolamento de filas (`ai` separado) e configurado `tries=3`/`timeout=120` para maior resiliência do chatbot.
  - Arquivos: `api/config/horizon.php`
  - Ref: incidente conta `teste@teste.com.br` (starter sem resposta de menu)

- [12:57] TEST [api]: Diagnóstico e validação E2E da conta `teste@teste.com.br`
  - Confirmado tenant em plano `starter` (`ai_enabled=false`), regras de auto-reply ativas e mensagens inbound WebChat sendo persistidas.
  - Confirmado gargalo operacional: fila `auto-reply` acumulada sem consumo contínuo (jobs pendentes/falhas antigas).
  - Após subir Horizon com `supervisor-auto-reply`, validado fluxo completo em ticket novo com resposta bot `Oi eu sou um menu`.
  - Ref: incidente conta `teste@teste.com.br` (starter sem resposta de menu)

- [13:11] FEAT [api]: Retrigger de menu de boas-vindas por keyword em interações subsequentes
  - `ChatAutoReplyResponder` passou a permitir retrigger da regra `is_welcome=true` por `trigger_text` (ex.: `menu`) quando `isFirstInteraction=false`.
  - Retrigger respeita `cooldown` da própria regra de boas-vindas para evitar repetição imediata.
  - Mantida proteção contra duplicidade na primeira interação (fluxo de boas-vindas inicial permanece separado).
  - Arquivos: `api/src/Domain/Chat/Services/ChatAutoReplyResponder.php`
  - Ref: solicitação de retrigger de menu no WebChat

- [13:11] TEST [api]: Cobertura de regressão para retrigger de boas-vindas
  - `ChatAutoReplyResponderTest` ganhou cenários de retrigger por keyword e bloqueio por cooldown em interações subsequentes.
  - Ajustado teste existente para explicitar cenário sem match de keyword.
  - Arquivos: `api/tests/Unit/Chat/ChatAutoReplyResponderTest.php`
  - Ref: solicitação de retrigger de menu no WebChat

- [13:13] FIX [api]: Bloqueio de keyword/auto-reply durante atendimento humano
  - `WebChatMessageController` passou a ignorar automação (IA e chatbot) quando o ticket está com `human_takeover_at` preenchido.
  - `ChatAutoReplyResponder` passou a bloquear processamento de palavras-chave e retrigger quando o ticket está sob atendimento humano, protegendo inclusive jobs já enfileirados.
  - Arquivos: `api/src/Domain/Chat/Http/Controllers/WebChatMessageController.php`, `api/src/Domain/Chat/Services/ChatAutoReplyResponder.php`
  - Ref: regra de negócio "atendimento humano desativa pesquisa por keywords"

- [13:13] TEST [api]: Regressões para bloqueio de automação em atendimento humano
  - `WebChatMessageControllerTest` valida que mensagem inbound é aceita, mas sem dispatch de IA e sem dispatch de `ChatAutoReplyRespondJob` quando `human_takeover_at` está ativo.
  - `ChatAutoReplyResponderTest` valida que o responder ignora keywords mesmo com match (`menu 1`) em ticket sob atendimento humano.
  - Arquivos: `api/tests/Feature/Chat/WebChatMessageControllerTest.php`, `api/tests/Unit/Chat/ChatAutoReplyResponderTest.php`
  - Ref: regra de negócio "atendimento humano desativa pesquisa por keywords"

- [16:25] FIX [api]: Bloqueio de análise de sentimento para tenants sem IA
  - `UpdateChatTicketAction` passou a validar `PlatformPlanEnforcementService::isAiEnabled()` antes de despachar `AiAnalyzeSentimentJob` no fechamento de ticket.
  - `AiAnalyzeSentimentJob` também passou a validar `isAiEnabled()` no `handle()`, bloqueando processamento de jobs antigos já enfileirados antes do deploy (defesa em profundidade).
  - Comportamento alinhado com a regra comercial: plano sem IA não executa jobs de IA (incluindo sentimento), mesmo com mensagens inbound válidas.
  - Arquivos: `api/src/Domain/Chat/Actions/UpdateChatTicketAction.php`, `api/src/Domain/Ai/Jobs/AiAnalyzeSentimentJob.php`
  - Ref: incidente de execução indevida de `Domain\Ai\Jobs\AiAnalyzeSentimentJob` em tenant sem IA

- [16:25] TEST [api]: Regressão para dispatch de sentimento condicionado ao plano
  - `ChatTicketActionsTest` ganhou cenário explícito garantindo que `AiAnalyzeSentimentJob` não é despachado quando `ai_enabled=false`.
  - Cenário positivo de fechamento com dispatch de sentimento passou a configurar plano com `ai_enabled=true` para refletir a nova regra de negócio.
  - `UpdateChatTicketActionTest` ajustado para nova dependência de `PlatformPlanEnforcementService`.
  - `AiAnalyzeSentimentJobTest` ganhou cenário explícito garantindo skip de processamento quando `ai_enabled=false`.
  - Arquivos: `api/tests/Unit/Chat/ChatTicketActionsTest.php`, `api/tests/Unit/Chat/Actions/UpdateChatTicketActionTest.php`, `api/tests/Feature/Domain/Ai/Jobs/AiAnalyzeSentimentJobTest.php`
  - Ref: incidente de execução indevida de `Domain\Ai\Jobs\AiAnalyzeSentimentJob` em tenant sem IA

- [17:05] FEAT [api]: Chatbot sem cooldown para permitir navegação livre no menu
  - `ChatAutoReplyResponder` deixou de aplicar cooldown nas regras de auto-reply (boas-vindas e regras por keyword).
  - Agora o usuário pode pedir `menu` repetidamente e voltar ao menu principal sem bloqueio temporal.
  - Mantidos os gates de segurança já existentes: takeover humano bloqueia automação e fluxo continua dependente de match de trigger.
  - Arquivos: `api/src/Domain/Chat/Services/ChatAutoReplyResponder.php`
  - Ref: ajuste de UX de navegação de etapas no chatbot

- [17:05] TEST [api]: Regressões para comportamento de chatbot sem cooldown
  - `ChatAutoReplyResponderTest` atualizado para validar que regras respondem mesmo com registro de cooldown ativo.
  - Cenário de retrigger do menu agora valida resposta mesmo com cooldown previamente gravado.
  - Ajustado cenário de primeira interação para não esperar persistência em `chat_auto_reply_cooldowns`.
  - Arquivos: `api/tests/Unit/Chat/ChatAutoReplyResponderTest.php`
  - Ref: ajuste de UX de navegação de etapas no chatbot

- [17:20] FIX [api]: Removida deduplicação de jobs de auto-reply por conteúdo
  - `ChatAutoReplyRespondJob` deixou de implementar `ShouldBeUnique` e removeu `uniqueId` por `ticket+body`.
  - Isso libera respostas repetidas para entradas iguais (ex.: `menu`, `1`) em sequência no mesmo ticket.
  - Arquivos: `api/src/Domain/Chat/Jobs/ChatAutoReplyRespondJob.php`
  - Ref: requisito de navegação livre sem bloqueio temporal no chatbot

- [17:20] TEST [api]: Cobertura para dispatch duplicado no auto-reply
  - `ChatAutoReplyResponderTest` ganhou cenário garantindo 2 dispatches quando o usuário envia o mesmo texto duas vezes no mesmo ticket.
  - Arquivos: `api/tests/Unit/Chat/ChatAutoReplyResponderTest.php`
  - Ref: requisito de navegação livre sem bloqueio temporal no chatbot
