# =====================================================
# Handlers: Users
# =====================================================

- name: "Testar conexão SSH agentflix"
  wait_for:
    port: 22
    delay: 2
    timeout: 10
    state: started
  ignore_errors: yes
