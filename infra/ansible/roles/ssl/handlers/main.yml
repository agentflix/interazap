# =====================================================
# Handlers: SSL
# =====================================================

- name: "Reload Nginx after SSL"
  service:
    name: nginx
    state: reloaded
