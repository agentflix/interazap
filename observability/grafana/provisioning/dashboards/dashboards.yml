apiVersion: 1

providers:
    - name: 'InteraZap'
      orgId: 1
      folder: 'InteraZap'
      folderUid: 'interazap'
      type: file
      disableDeletion: false
      updateIntervalSeconds: 30
      options:
          path: /var/lib/grafana/dashboards
