.DS_Store
node_modules/.bin/browserslist
/node_modules
/code-base
/testsprite_tests

# --- electron/ ---
/electron/node_modules
/electron/dist          # TypeScript compilado (gerado por tsc)
/electron/app           # Renderer Angular copiado pelo prebuild
/electron/release       # Artefatos de distribuição (.dmg, .exe, .AppImage)
# Certificados de code signing — NUNCA versionar
electron/*.p12
electron/*.pfx
electron/*.key
electron/*.pem

# --- gateway/ ---
/gateway/node_modules
/gateway/dist
/gateway/.env
/gateway/logs
gateway/logs/gateway-events.log
/gateway/coverage

# --- api/ ---
api/coverage.xml
api/.env

# --- Docker ---
.env.docker

# --- IA (regenerável via scripts/setup-ai.sh) ---
/.opencode
CLAUDE.md
