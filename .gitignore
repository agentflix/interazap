.DS_Store
node_modules/.bin/browserslist
/node_modules
/code-base
/testsprite_tests

# --- electron/ ---
/electron/node_modules
# TypeScript compilado (gerado por tsc)
/electron/dist
# Renderer Angular copiado pelo prebuild
/electron/app
# Artefatos de distribuição (.dmg, .exe, .AppImage)
/electron/release
# Certificados de code signing — NUNCA versionar
electron/*.p12
electron/*.pfx
electron/*.key
electron/*.pem

# --- gateway/ ---
/gateway/node_modules
/gateway/dist
/gateway/.env
/gateway/logs
gateway/logs/gateway-events.log
/gateway/coverage

# --- api/ ---
api/coverage.xml
api/.env

# --- ladding/ ---
/landing/node_modules

# --- Docker ---
.env.docker

# --- IA (regenerável via scripts/setup-ai.sh) ---
CLAUDE.md

# --- Backup files (never commit) ---
*.bak
package-lock.json
# Assets do bootstrap AI-First (podem ser commitados se desejado)
# builder/prevec-assets/
# BOOTSTRAP/prevec-assets/
/.playwright-mcp

# PREVEC session files — comunicacao temporaria entre agents
.context/.session/
